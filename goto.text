- there may be a situation where you need to add "@repo/db" as "dependencies" and not as "devDependencies" as KIRAT did it.
- find a way to add './output' folder for generated client for prisma.
- message queue to database

-add tailwind
-use react forms or react hook forms
-add dark mode button now. because you will not get better oppertunity to do it.